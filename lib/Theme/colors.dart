import 'package:flutter/material.dart';

class HColors {
  HColors._();

// app basic colors

  static const Color primary = Color(0xff008000);
  static const Color secondary = Color(0xff9fc6bc);
  static const Color tertiary = Color(0xff3964FE);

  //label  colors
  static const Color labelPrimary = Color.fromARGB(255, 99, 99, 99);

// background colors
  static const Color light = Color.fromARGB(255, 255, 255, 255);
  static const Color dark = Color.fromARGB(255, 0, 0, 0);
}
